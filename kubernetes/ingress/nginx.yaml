apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: ingress-nginx
spec:
  ingressClassName: nginx
  rules:
    - http:
        paths:
          - path: "/"
            pathType: Prefix
            backend:
              service:
                name: client
                port:
                  number: 4200
          - path: "/api"
            pathType: Prefix
            backend:
              service:
                name: api
                port: 
                  number: 5000
          - path: "/webapi"
            pathType: Prefix
            backend:
              service:
                name: webapi
                port:
                  number: 9000
